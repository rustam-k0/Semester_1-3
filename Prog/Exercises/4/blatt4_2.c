#include <stdio.h>
#include <stdbool.h>

int main() {
    int x = 1, y = 2;
    bool z = true;

    printf("%d\n", ( (y++) * 5 ) + x );      // 11
    printf("%d\n", ( x * 5 ) % ( ++y ) );    // 1
    printf("%d\n", ( x++ ) - ( y-- ) );      // -3
    printf("%d\n", ( (x * 5) < y ) || ( z && (x > y) ) ); // 0
    printf("%d\n", ( x = ( y = (y + 1) ) ) );            // 4

    return 0;
}

/*
Главные правила этого упражнения:

1. Выполняй выражения строго по порядку — после каждой строки x и y изменяются.
2. Различай постинкремент и прединкремент:
       y++ возвращает старое значение, потом увеличивает.
       ++y увеличивает сразу и возвращает новое.
3. Всегда расставляй скобки, показывая реальную последовательность вычислений.
4. Учитывай приоритет операторов:
       * / %  → раньше
       + -    → позже
       < > <= >= → после арифметики
       && раньше чем ||
       = самый правый (право-ассоциативный)
5. Помни: логические выражения вычисляются слева направо с коротким замыканием.
6. Оператор присваивания возвращает значение присваиваемого выражения.
*/

}
